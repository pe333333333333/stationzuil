import csv
import psycopg2
from datetime import datetime as dt

def create_connection():
    """
    Maakt verbinding met de database en maakt de standaard tabellen aan, mits
    deze nog niet bestaan.

    :returns De database verbinding
    """
    conn = psycopg2.connect(
        dbname='Stationszuil',
        user='postgres',
        password='0000',
        host='localhost',
        port='5432'
    )
    return conn

def insert_reviews(conn):
    """
    Leest het reviews bestand uit, leegt het bestand, en zet vervolgens alle
    reviews in de database.

    :param conn: De database verbinding
    """
    try:
        with open('reviews.csv', 'r+') as reviews_file:
            reader = csv.reader(reviews_file)
            reviews = list(reader)
            reviews_file.truncate(0)

            if reviews:
                print('Alleproject statsionszuil reviews importeren in de database...')
                with conn.cursor() as cur:
                    for review in reviews:
                        cur.execute(
                            "INSERT INTO review (bericht, naam, station, datum) "
                            "VALUES (%s, %s, %s, %s)", review
                        )
                    conn.commit()
                    print('Klaar!\n')
            else:
                print('Geen reviews om te importeren.')
    except FileNotFoundError:
        print('Geen reviews om te importeren.')

def get_reviews(conn):
    """
    Returned een lijst met alle reviews die nog niet zijn beoordeeld.

    :param conn: De database verbinding
    :returns: Een lijst met tuples met daarin de gegevens van de review
    """
    with conn.cursor() as cur:
        cur.execute(
            "SELECT * FROM review r "
            "WHERE NOT EXISTS (SELECT * FROM beoordeling b WHERE b.review_id = r.id);"
        )
        return cur.fetchall()

def get_moderator_name(conn, moderator_email):
    """
    Controleert of de moderator al bestaat in de database en returned de naam
    van deze moderator als dat zo is. Zo nee, dan wordt er niks gereturned.

    :param conn: De database verbinding
    :param moderator_email: De e-mail van de moderator
    :return: De naam van de moderator of niks
    """
    with conn.cursor() as cur:
        cur.execute("SELECT naam FROM moderator WHERE email = %s;", [moderator_email])
        data = cur.fetchone()
        return data[0] if data else None

def insert_moderator(conn, moderator_email, moderator_name):
    """
    Zet een nieuwe moderator in de database.

    :param conn: De database verbinding
    :param moderator_email: De e-mail van de moderator
    :param moderator_name: De naam van de moderator
    """
    with conn.cursor() as cur:
        cur.execute("INSERT INTO moderator (email, naam) VALUES (%s, %s);", [moderator_email, moderator_name])
        conn.commit()

def insert_beoordeling(conn, beoordeling):
    """
    Zet een beoordeling in de database.

    :param conn: De database verbinding
    :param beoordeling: Een tuple met de beoordelinggegevens.
    """
    with conn.cursor() as cur:
        cur.execute("INSERT INTO beoordeling (review_id, goedgekeurd, datum, moderator_email) VALUES (%s, %s, %s, %s)", beoordeling)
        conn.commit()

# Hoofdlogica
conn = create_connection()
insert_reviews(conn)

print('Moderator login\n---------------')
while True:
    moderator_email = input('Wat is uw e-mailadres?\n')
    if not moderator_email.strip():
        print('Vul uw e-mailadres in.')
        continue
    moderator_name = get_moderator_name(conn, moderator_email)
    if moderator_name:
        print(f'Welkom terug, {moderator_name}!\n')
    else:
        while True:
            moderator_name = input('Wat is uw naam?\n')
            if not moderator_name.strip():
                print('Vul uw naam in.')
                continue
            insert_moderator(conn, moderator_email, moderator_name)
            print(f'Welkom, {moderator_name}!\n')
            break
    break

print('Beoordeel de volgende reviews:\n------------------------------')
beoordelingen = ['Afkeuren', 'Goedkeuren']
antwoorden = ['afgekeurd', 'goedgekeurd']

for review in get_reviews(conn):
    print(f'{review[2]} zei op {dt.strftime(review[4], "%d %B %Y, %H:%M:%S")} in {review[3]}:\n{review[1]}')
    index = int(input(f'Goedkeuren (1) of Afkeuren (0)? [1/0]: '))

    # Verzamel datum en stel beoordeling samen
    beoordeling_datum = dt.now()
    beoordeling = (review[0], bool(index), beoordeling_datum, moderator_email)

    # Schrijf naar database
    insert_beoordeling(conn, beoordeling)
    print(f"Succesvol {antwoorden[index]}.\n")

print(f"Alle reviews zijn beoordeeld. Bedankt {moderator_name}!")
conn.close()
